#!/usr/bin/env bash

#if [ -f $1/_config/php.ini && -f $1/_config/php-fpm.ini && -f $1/_config/nginx.conf.erb ]; then
if [ -f $1/_config/php.ini ]; then
	echo "Nginx/PHP"
	exit 0
else 
	echo "No"
	exit 1
fi
